# Catch curve estimates of total mortality at age (Z)

Catch curve estimates of total mortality at age (Z)

## Usage

``` r
acc(object, ...)
```

## Arguments

- object:

  An input object of the right class

## Examples

``` r
data(ple4)
```
