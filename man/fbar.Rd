% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/genericMethods.R, R/FLStock.R
\name{fbar}
\alias{fbar}
\alias{fbar,FLStock-method}
\title{Mean fishing mortality over a selected age range}
\usage{
fbar(object, ...)

\S4method{fbar}{FLStock}(object, min = range(object, "minfbar"), max = range(object, "maxfbar"))
}
\arguments{
\item{object}{An \linkS4class{FLStock} object.}

\item{min}{Integer. Minimum age to include in the mean. Defaults to the
value stored in \code{range(object, "minfbar")}; if that value is \code{NA}, the
smallest age in the object (\code{range(object, "min")}) is used instead.}

\item{max}{Integer. Maximum age to include in the mean. Defaults to the
value stored in \code{range(object, "maxfbar")}; if that value is \code{NA}, the
largest age in the object (\code{range(object, "max")}) is used instead.}
}
\value{
An \linkS4class{FLQuant} with the \code{quant} dimension collapsed to
one, containing the arithmetic mean of \code{\link[=harvest]{harvest()}} over ages
\code{min} to \code{max} inclusive, as computed by \code{\link[=quantMeans]{quantMeans()}}.
}
\description{
Computes the arithmetic mean of the \code{\link[=harvest]{harvest()}} slot over a
contiguous range of ages.
}
\details{
Overriding \code{min} and \code{max} directly allows a one-off calculation over a
different age range without modifying the object.

The method accepts both \code{"f"} and \code{"hr"} as valid units for the
\code{\link[=harvest]{harvest()}} slot and applies \code{\link[=quantMeans]{quantMeans()}} identically in
both cases.
}
\section{Methods (by class)}{
\itemize{
\item \code{fbar(FLStock)}: The \code{minfbar} and \code{maxfbar} entries of the \code{range} slot are
specific to the \linkS4class{FLStock} class, and define the reference age range
for fishing-mortality calculations. These are set when the \linkS4class{FLStock}
object is created or read, and can be altered using .

}}
\examples{
data(ple4)

# Default fbar using minfbar/maxfbar from range slot
fbar(ple4)

# Override age range for a one-off calculation
fbar(ple4, min = 3, max = 6)

# Inspect the reference age range stored in the object
range(ple4, c("minfbar", "maxfbar"))

}
\seealso{
\code{\link[=harvest]{harvest()}}, \code{\link[=quantMeans]{quantMeans()}}, \code{\link[base:range]{base::range()}}

\linkS4class{FLStock}
}
\author{
FLR Team
}
\keyword{methods}
