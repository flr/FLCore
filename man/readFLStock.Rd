% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.FLStock.R
\name{readFLStock}
\alias{readFLStock}
\alias{writeFLStock}
\title{Read and write FLStock objects from assessment exchange files}
\usage{
readFLStock(file, type = "VPA", name, desc = paste("Imported from a", 
  type, "file. (", file, "). ", date()), m = 0.2, quant = "age", 
  quiet = TRUE, no.discards = FALSE, harvest.units, sep = "", 
  na.strings = "NA")

writeFLStock(FLStock, output.file = FLStock@name, type = "VPA")
}
\arguments{
\item{file}{Path to the input file.}

\item{type}{File format to read or write. Supported input types are
\code{"VPA"}, \code{"Adapt"}, \code{"PA"} and \code{"CSA"}.\cr
\code{writeFLStock()} currently supports \code{"VPA"}.}

\item{name}{Optional name to assign to the returned object.}

\item{desc}{Optional description to assign to the returned object.}

\item{m}{Natural mortality value used by some input readers.}

\item{quant}{Name of the first dimension to assign to imported \code{FLQuant}
slots.}

\item{quiet}{Should file reading be quiet?}

\item{no.discards}{Should discards-related slots be filled with zero after
import?}

\item{harvest.units}{Units to assign to the \code{harvest} slot. When missing,
a sensible default is selected from \code{type}.}

\item{sep}{Field separator passed to the file reader.}

\item{na.strings}{Character vector used to identify missing values in the
input.}

\item{FLStock}{An \code{\link{FLStock}} object to write.}

\item{output.file}{Path to the output file.}
}
\value{
\code{readFLStock()} returns an \code{\link{FLStock}} object.
\code{writeFLStock()} returns the value produced by the format-specific
writer, and is mainly used for its side effect of writing files.
}
\description{
\code{readFLStock()} imports stock assessment input files into an
\code{\link{FLStock}} object. \code{writeFLStock()} writes an
\code{\link{FLStock}} to a supported exchange format.
}
\seealso{
\linkS4class{FLStock}, \link{readFLIndices}, \link{readVPAFile}
}
\author{
The FLR Team
}
