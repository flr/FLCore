% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oem.R
\docType{methods}
\name{survey}
\alias{survey}
\alias{survey-methods}
\alias{survey,FLStock,FLIndex-method}
\alias{survey,FLStock,FLIndexBiomass-method}
\alias{survey,FLStock,missing-method}
\alias{survey,FLStock,FLIndices-method}
\title{Generate simulated survey observations of abundance}
\usage{
survey(object, index, ...)

\S4method{survey}{FLStock,FLIndex}(
  object,
  index,
  sel = sel.pattern(index),
  ages = dimnames(index)$age,
  timing = mean(range(index, c("startf", "endf"))),
  index.q = index@index.q,
  stability = 1
)

\S4method{survey}{FLStock,FLIndexBiomass}(
  object,
  index,
  sel = sel.pattern(index),
  ages = ac(seq(range(index, c("min")), range(index, c("max")))),
  timing = mean(range(index, c("startf", "endf"))),
  catch.wt = index@catch.wt,
  index.q = index@index.q,
  stability = 1
)

\S4method{survey}{FLStock,missing}(
  object,
  sel = catch.sel(object),
  ages = dimnames(sel)$age,
  timing = 0.5,
  index.q = 1,
  biomass = FALSE,
  stability = 1
)

\S4method{survey}{FLStock,FLIndices}(object, index, ...)
}
\arguments{
\item{object}{An \code{FLStock} operating model from which to draw the survey.}

\item{index}{An \code{FLIndex}, \code{FLIndexBiomass}, or \code{FLIndices}
defining the survey design; if missing, a generic survey is constructed.}

\item{...}{Additional arguments passed to methods.}

\item{sel}{Selectivity-at-age pattern to apply; defaults to
\code{sel.pattern(index)}.}

\item{ages}{Character vector of ages to include; defaults to all ages in
\code{index}.}

\item{timing}{Fraction of the year at which the survey takes place (0--1);
defaults to the midpoint of the \code{startf}/\code{endf} range entries.}

\item{index.q}{Catchability of the survey; defaults to \code{index@index.q}.}

\item{stability}{Hyperstability/hyperdepletion exponent applied to abundance;
1 (default) gives a linear response.}

\item{catch.wt}{Mean weight-at-age used to convert numbers to biomass for
\code{FLIndexBiomass} surveys; defaults to \code{index@catch.wt}.}

\item{biomass}{Logical; if \code{TRUE} and \code{index} is missing, returns
an \code{FLIndexBiomass} object; otherwise an \code{FLIndex}.}
}
\value{
An \code{FLIndex} or \code{FLIndexBiomass} object with the
\code{index} slot populated by the simulated survey observations, or a
list of such objects when \code{index} is of class \code{FLIndices}.
}
\description{
Creates a simulated observation of abundance-at-age or biomass index from
an \code{FLStock} object representing the operating model (OM), replicating
the sampling process of a scientific survey. The result is returned as an
\code{FLIndex} or \code{FLIndexBiomass} object with the \code{index} slot
populated.
}
\section{Generic function}{
 survey(object, index, ...)
}

\examples{
data(ple4)
data(ple4.index)
# CONSTRUCT a survey from stock and index
survey(ple4, ple4.index)
# Create FLIndexBiomass
ple4.biom <- as(ple4.index, "FLIndexBiomass")
survey(ple4, ple4.biom)
data(ple4)
survey(ple4)
survey(ple4, biomass=TRUE)
}
\seealso{
\linkS4class{FLIndex}, \linkS4class{FLIndexBiomass},
\linkS4class{FLStock}, \link{cpue}, \link{index}
}
\author{
The FLR Team
}
\keyword{methods}
